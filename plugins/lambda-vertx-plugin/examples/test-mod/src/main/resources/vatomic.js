var vatomic = vatomic || {}

vatomic.core = {

    vertx     : require('vertx'),
    container : require('vertx/container'),
    console   : require('vertx/console')
    //config    : container.config,
    //event_bus : vertx.eventBus,

    //end : function( payload ) {
        //event_bus.publish("module-return", payload)
    //}
}
